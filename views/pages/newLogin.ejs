<!DOCTYPE html>
<html lang="en">

    <head>
        <title>EventFULL</title>
        
        <nav class="navbar navbar-dark fixed-top" style="background-color: #7ccba2cb; ">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">EventFULL</a>
            </div>
        </nav>    

        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet"
            integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3"
            crossorigin="anonymous"></script>
    
    </head>
    
    <body style="background-image: url()">
        
        <p style=" justify-content: center; margin-top: 150px; display: flex; font-weight: bold; font-size: 50px; ">
            Καλωσήρθατε στο EventFULL
        </p>  
    
        <div style="display: flex; justify-content: center; margin-top: 100px; ">
            <form>
                <div class="mb-3">
                    <label for="username" class="form-label">Όνομα Χρήστη</label>
                    <input type="text" class="form-control" id="username">
                    <div class="form-text"></div>
                </div>
                <div class="mb-3" >
                    <label for="password" class="form-label">Κωδικός Πρόσβασης</label>
                    <input type="password" class="form-control" id="password">
                    <div class="form-text">Δεν θα κοινοποιήσουμε ποτέ τον κωδικό σας.</div>
                </div>
                
                <div style="display: flex; justify-content: end;">
                    <a href="javascript:delay('/home')">
                    <button id="loginBtn" class="btn btn-success" type="button" style="background-color:#16874bf6">Είσοδος</button>
                    </a>
                </div>
               
                <div style="text-align: center; margin-top: 5px;">
                    <span> Δεν έχετε ήδη λογαριασμό; <a href="/register"> Εγγραφείτε εδώ </a>
                    </span>
                </div>
            </form>
        </div>
    
    </body>
    
    <script>
        var username = document.getElementById("username")
        var password = document.getElementById("password")
        var loginBtn = document.getElementById("loginBtn")

        loginBtn.addEventListener("click", function(req, res){
            var data={
                "username":username.value,
                "password":password.value
            }
           
            // Sending and receiving data in JSON format using POST method
            //
            var xhr = new XMLHttpRequest();
            var url = "/loginData";
            xhr.open("POST", url, true);
            xhr.setRequestHeader("Content-Type", "application/json");
            xhr.onreadystatechange = function () {
                if (xhr.readyState === 4 && xhr.status === 200) {
                    console.log(data.username + ", " + data.password);
                    sessionStorage.setItem("JWT",this.responseText)
                    authRedirect('profile');
                }
            };
            var dataJSON = JSON.stringify({data});
            xhr.send(dataJSON);
            
        });
         

        function authRedirect(page){
            axios.post('/'+page, {}, {
                headers: {
                    
                    'Authetication': 'Bearer '+ sessionStorage.getItem('JWT')
                }
            });
        }

        function delay (URL) {
            setTimeout( function() { window.location = URL }, 500 );
        }

    </script>
</html>